process is foo(a is ref num) is action
{
	a = sum(a, a);
}

func is foo(a is ref num) is float
{
	a = 23;
	return a;
}

a => 2111;
main
{

	For(1, func(a), 2, el => {
		console(el, ' ');
		skip;
	});
	
	* Fact is foo(a is num) is num
	* {
	* 	fact => 1;
	* 
	* 	For(1, a, 1, i => {
	* 		fact = mult(fact, i);
	* 	});
	* 
	* 	return fact;
	* };
	
	return 0;
}