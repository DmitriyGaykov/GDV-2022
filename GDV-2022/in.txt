f is foo(a is ref float) is float
{
	(a > 2) ?
	Lie
	{
		a = 2;
	}

	
	(a == 23) ?
	Truth
	{
		a = sum(23, 1) & 23;
	}

	console(`A теперь имеет значение: `, a);
	return a & a;
}

main
{
	a => 2222222222222220000000000000000.23;

	cb => sum('1', 23);
	
	For(1, 23, 2, el => {
		el = el;
	});
	
	b is float = 22.2 & 'g';
	f(a);
	char => '$';

	b = a & 2.3 | 1 | ~23;
	
	return 0;
}
