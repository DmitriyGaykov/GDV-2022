
* Сделать:
* сделать проверку на ограничения в числе
* сделать проверку на то, что нельзя был присвоить функцию, как значение
* сделать проверку параметров. Ссылки только идентификаторы
* область видимости с action
* приведение типов. С литералами не работает

process is foo(a is ref num) is action
{
	a = sum(a, a);
}

func is foo(a is num) is float
{
	return a;
}

main 
{
	a => 2;
	res => func(sum(1, mult(2.2, func(a))));
	
	* Fact is foo(a is num) is num
	* {
	* 	fact => 1;
	* 
	* 	For(1, a, 1, i => {
	* 		fact = mult(fact, i);
	* 	});
	* 
	* 	return fact;
	* };
	
	return 0;
}