* !!! Polish.cpp с функциями разобраться надо! 

* Сделать:
* сделать проверку на используемые в выражениях литералы и идентификаторы
* сделать проверку на ограничения в числе
* сделать проверку на то, что нельзя был присвоить функцию, как значение
* (foo()) ? - ошибка

* Сделано и надо тестить:
* сделать замену в имени "." на "_"
* сделать значение лексеме в таблице идентификаторов
* сделать проверку, есть ли return в main или в функциях
* сделать проверку на тип данных. Оператор is
* (i V i) ? T{}
* T{} - не работает
* return убирает область видимости из-за чего все ломается 
* сделать проверку комментария в символьном литерале '*'

* Заметки:
* variable => identificator или lexema
* return 100% должен быть в конце
* в параметры вызываемой функции, в условиях, console() можно передавать лишь идентификаторы, или литералы, или вызываемую функцию 

* For(start, end, step, el => {func_body});
* (1 > 1) ? Truth {} Lie {}

* Объявление: a => value | identificator; a is num; a is symb;

@import pow

Fact is foo(a is num) is num
{
	fact => 1;

	For(1, a, 1, i => {
		fact = mult(fact, i);
	});

	return fact;
}

main 
{
	pow is foo(number is num, power is num) is num
	{
		result => 1;

		For(1, power, 1, i => {
			result = mult(result, number);
		});

		return result;
	};

	console(pow(2, 3));
	return 0;
}