Fact is foo(a is num) is num
{
    res => 1;
	
	For(1, a, 1, el => 
	{
        res = mult(res, el);
    });

    return res;
}

main
{
    a => Fact(3);
	(a == 6) ? 
	{
		console(`a равно 6`);
	} : {
		console(`a не равно 6`);
	}

	`\nПодключение функции`
	@import round;

	half is float;
	half = .5 & 1;
}